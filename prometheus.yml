# prometheus.yml
global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'jaeger-spanmetrics'
    # This tells Prometheus to scrape the metrics endpoint that Jaeger will expose.
    # 'jaeger' is the service name from our docker-compose.yml.
    static_configs:
      - targets: ['jaeger:8889']